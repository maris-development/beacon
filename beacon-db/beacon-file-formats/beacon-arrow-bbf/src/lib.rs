//! `beacon-arrow-bbf` provides the DataFusion integration for the Beacon Binary
//! Format (BBF), wrapping the `beacon-binary-format` reader as a DataFusion with
//! pruning, stream sharing and metrics.

pub mod datafusion;
