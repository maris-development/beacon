[package]
name = "beacon-arrow-atlas"
version.workspace = true
edition = "2024"

rust-version.workspace = true

[dependencies]
# The reader. Pinned exactly in the workspace: the container embeds
# `array-format` files verbatim, so a change to those bytes is a format change.
atlas-rust = { workspace = true }

object_store = { workspace = true }
datafusion = { workspace = true }
arrow = { workspace = true }
async-trait = { workspace = true }
anyhow = { workspace = true }
futures = { workspace = true }
indexmap = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
ndarray = { workspace = true }
chrono = { workspace = true }
moka = { workspace = true }

beacon-datafusion-ext = { path = "../../beacon-datafusion-ext" }
beacon-nd-array = { path = "../beacon-nd-array" }
beacon-common = { path = "../../beacon-common" }

[dev-dependencies]
tempfile = { workspace = true }
