# Keep the Docker build context small. The Dockerfile COPYs individual crate
# directories plus Cargo.{toml,lock} and rust-toolchain.toml, so none of the entries
# below are needed inside the image — excluding them avoids shipping multi-GB
# directories (notably target/ and data/) to the daemon on a clean build.
.git
target
data
logs
mappings
docs
benchmarks
test-datasets
vocab-generation-tooling
beacon-studio
beacon-example
beacon-py
integration-tests
**/.venv
**/node_modules
# JS build outputs are regenerated inside the webui Docker stage.
beacon-datalake-clients/beacon-ts/dist
beacon-datalake-clients/beacon-web/dist
**/*.pyc
__pycache__
*.ipynb
