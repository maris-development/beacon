name: Publish beacon-datalake-cli

# Publishes the `beacon-datalake-cli` Python package to PyPI.
#
# Release candidates: a pre-release tag (v2.0.0-rc.1) publishes as PyPI version 2.0.0rc1 — PyPI
# normalizes the SemVer suffix to PEP 440. No dist-tag equivalent is needed: pip ignores
# pre-releases unless asked, so `pip install beacon-datalake-cli` stays on the last stable release
# and RC users opt in with `pip install --pre beacon-datalake-cli`.

on:
  push:
    tags:
      # Covers stable (v1.8.0) and pre-release (v2.0.0-rc.1) tags alike.
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish, e.g. 1.8.0 or 2.0.0-rc.1. Empty = latest stable v* tag."
        required: false
        type: string
        default: ""

permissions:
  contents: read

defaults:
  run:
    working-directory: beacon-datalake-clients/beacon-datalake-cli

jobs:
  build:
    name: Test and build distributions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # full history + tags, so we can find the latest tag

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package (with dev tools)
        run: python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check .

      - name: Test
        run: python -m pytest

      - name: Resolve version
        id: version
        env:
          EVENT: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          if [ "$EVENT" = "push" ]; then
            version="$REF_NAME"                          # the pushed release tag, e.g. v1.8.0
          elif [ -n "$INPUT_VERSION" ]; then
            version="$INPUT_VERSION"                     # from the manual input
          else
            # Latest *stable* tag. git's version sort has no SemVer pre-release semantics and ranks
            # v2.0.0-rc.1 above v2.0.0, so drop pre-releases here; to republish an RC by hand, name
            # it explicitly in the `version` input.
            tag=$(git tag --list 'v*' --sort=-v:refname | grep -vE '^v.*-' | head -n1)
            [ -n "$tag" ] || { echo "::error::No stable v* tags found; provide a version input."; exit 1; }
            version="$tag"                               # latest stable release tag
          fi
          version="${version#v}"                         # strip the leading 'v' -> X.Y.Z
          [ -n "$version" ] || { echo "::error::Could not resolve a version."; exit 1; }
          echo "Resolved version: $version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Set package version
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          python - <<'PY'
          import os, re, pathlib
          v = os.environ["VERSION"]
          if not re.fullmatch(r"\d+\.\d+\.\d+([.\-+a-zA-Z0-9]*)?", v):
              raise SystemExit(f"Version '{v}' does not look like a valid version")
          path = pathlib.Path("pyproject.toml")
          text = path.read_text()
          new = re.sub(r'(?m)^version = "[^"]*"', f'version = "{v}"', text, count=1)
          if new == text:
              raise SystemExit("Could not find a 'version = \"...\"' line in pyproject.toml")
          path.write_text(new)
          print("pyproject version set to", v)
          PY

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade build
          python -m build

      - name: Check distributions
        run: |
          python -m pip install --upgrade twine
          python -m twine check dist/*

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: beacon-datalake-cli-dist
          path: beacon-datalake-clients/beacon-datalake-cli/dist/*
          if-no-files-found: error

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/beacon-datalake-cli
    permissions:
      id-token: write # OIDC token for PyPI Trusted Publishing
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: beacon-datalake-cli-dist
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          # A release tag may fire this more than once (e.g. a re-run) — don't
          # fail if that exact version is already on PyPI.
          skip-existing: true
